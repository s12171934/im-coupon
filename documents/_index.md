# im-coupon 문서

> 이 저장소의 **결정·진행경과·설계방침**의 SSOT. 코드와 git 이력에는 이 맥락이 없다.
> 이 저장소에서 맥락에 의존하는 작업은 이 파일을 먼저 읽고 시작한다 (`documents-first` 스킬).
> 갱신 — 2026-09-13

## 이 저장소

iM뱅크 주최 `2026 AI Blockchain Challenge in Daegu` 공모전(접수 마감 2026-09-20 23:59) 제출용
**소비 데이터 기반 상생형 쿠폰 프로토타입**이다. 발급 트랙과 소비 트랙 두 갈래를 시연 가능한 형태로 만드는 것이 범위다.

## 문서 목록

- [KAN-24 개인화 신호 로직과 노드 흐름](personal-fit-flow.md) — 준비·조회 다이어그램, 22개 노드와 분기, 계산 예제 및 서비스 연결 범위.

- [쿠폰 도메인 규칙](쿠폰-도메인-규칙.md) — 발급 트리거와 발급 대상 가중치, 혜택 이분화, 이중 기한, 공개 풀과 점유, 설계를 제약하는 법·재원 조건. **무엇을 만드는가**를 물을 때 읽는다
- [프로토타입 범위](프로토타입-범위.md) — 포함과 제외의 경계, 목 데이터 추상화 방침, 파라미터로 빼 둘 값, 만드는 순서. **어디까지 만드는가**를 물을 때 읽는다
- [저장소 구조와 기술 스택](저장소-구조와-기술-스택.md) — 워크스페이스 경계와 의존 방향, 공유 패키지의 역할, 테스트 도구와 Turborepo 태스크. 어디에 코드를 둘지, 어떤 명령으로 검증할지 물을 때 읽는다
- [지라 티켓 연결](지라-티켓-연결.md) — 티켓과 이 저장소 문서의 대응, 새 티켓을 만드는 규칙, 지라 쓰기 경계. 티켓을 열거나 만들 때 읽는다
- [KAN-13 프로토타입 쿠폰 발급 구현 — 설계문서](KAN-13/design.md) — 에픽 KAN-13 의 구현 착수 전 설계. 랜덤 신호 하나로 발급 1건을 만들어 내 쿠폰 화면까지 보이는 범위의 브랜치 스택·API 계약·테스트 계획이 여기 있다

- [S6 행동 이력 기반 signal 추가 — 설계](behavior-signal-design/design.md) — randomSignal과 같은 signal 인스턴스 추가 설계. web·신규 API·DB 제외. 에픽 KAN-22, 설계 KAN-23·구현 KAN-24의 브랜치·PR 매핑

- [KAN-24 personalFitSignal 구현 착수 계획](behavior-signal-design/implementation-plan.md) — 구현 Draft의 체크리스트와 미실행 상태. 상세 계약은 S6 설계문서를 따른다.

- [KAN-24 personalFit 최종 검증 기록](personal-fit-validation.md) — CB1~3 코드의 TC-01~15 추적성, 루트 검증 결과, RED·타입검사 범위와 재사용 근거.

<!-- 등록 형식: - [문서 제목](파일명.md) — 이 문서가 무엇을 결정·기록하는지 한 줄 -->

## 아직 기록되지 않은 것

결정은 내려져 있으나 이 폴더에 아직 문서로 남지 않은 주제들이다. 해당 주제의 작업을 시작할 때 문서로 만들면서 함께 정리한다.

- [ ] **문제정의 근거 수치** — 제안 서류에 쓸 대구 상권·소비 실증 수치. 구현에 걸리지 않으므로 후순위다
- [ ] **어뷰징 방지 룰의 판별식** — 방지 대상과 방향은 [쿠폰 도메인 규칙](쿠폰-도메인-규칙.md) 에 있으나, 탐지 조건의 수치가 정해지지 않았다

## 진행경과

- 2026-09-13 — KAN-24 CB1~3의 독립 personalFit 입력·준비 계산·시민별 조회 구현과 주요 한국어 흐름 주석을 완료했다. 루트 typecheck/test/build가 통과했으며 API 197개를 새 실행했다. 상세 근거는 [최종 검증 기록](personal-fit-validation.md)에 남겼다. 서비스 활성화는 범위 밖이며 CB4 검증 기록은 독립 리뷰 한 라운드(A 발견 0건, B P3 1건 정정)를 완료하여 본 커밋에 포함했다. 아래 착수 기록은 구현 전 시점의 이력이다.

- 2026-09-13 — S6 설계의 CP-01~03 구현계획을 상세화했다. 타입·입력 계약, 정제·벡터·점수 준비, 동기 조회의 작업 순서·오류 처리·완료 기준과 TC-10~15를 추가했다. 앱 구현·테스트는 미실행이다.

- 2026-09-13 — S6 설계를 signal 하나로 축소하고 Jira MCP로 에픽 KAN-22·설계 KAN-23·구현 KAN-24를 생성했다. 설계 PR #19 → 구현 PR #20을 gh-stack의 stack #21로 연결했다. 구현 PR #20은 작업 계획만 담은 Draft다. 앱 구현·테스트는 미실행이다.

- 2026-09-09 — KAN-13 스택 전체에 최신 main의 소비 트랙을 병합했다. 소비 쪽 이름을 유지하고 발급 계약을 `IssuedCoupon`·`IssuedCouponStatus`·`IssuanceRequest`, 발급 저장소를 `issued-coupons`로 구분했다. 기존 발급 데이터 이동·재시작·소비 초기화 후 보존을 검증하고, 세 화면 경로를 함께 연결했다. 최상단 단위·통합 테스트 251건, 타입 검사·빌드와 브라우저 E2E 6건이 통과했다.

- 2026-09-05 — 모노레포 골격을 세웠다. `apps/web`·`apps/api`·`packages/contracts`·`packages/db`·`e2e` 다섯 워크스페이스가 서고, 화면에서 API 를 거쳐 JSON 파일 DB 까지 닿는 경로 하나(저장소 상태 표시)가 관통한다. 단위 테스트 9+2+2건과 e2e 1건이 통과하며, `pnpm dev` 한 명령으로 전체가 뜬다. 쿠폰 도메인 로직은 아직 없다.
- 2026-09-05 — 기획 원본을 저장소로 수거했다. [쿠폰 도메인 규칙](쿠폰-도메인-규칙.md) 과 [프로토타입 범위](프로토타입-범위.md) 를 만들어, 발급·소비 규칙과 목 데이터·파라미터 방침이 저장소 안에서 선다. [지라 티켓 연결](지라-티켓-연결.md) 로 티켓 대응 관계도 함께 들여왔다. 구현은 아직 시작하지 않았다.
- 2026-09-05 — 개발 서버가 자기 워크트리 삭제 후에도 살아남아 포트를 붙드는 문제를 막았다. API 는 자기 소스 디렉터리가 사라지면 스스로 종료하고, 포트 선점 실패는 점유자를 찾는 안내로 바뀌었으며, e2e 는 남이 띄운 서버를 재사용하지 않는다. `.gitignore` 도 산출물·테스트 결과·환경 변수·편집기·운영체제 파일을 모두 덮도록 정리했다.
- 2026-09-05 — 문서에 다이어그램을 넣었다. 쿠폰 라이프사이클 상태기계, 발급 대상 결정 파이프라인, 혜택 이분화 경로, 워크스페이스 의존 방향, 개발 서버 기동·수명, 만드는 순서의 선행 관계, 목 데이터 교체 지점, 티켓 계층 여덟 장이다. 형식은 Mermaid 인라인으로 고정했고, 규칙은 [저장소 구조와 기술 스택](저장소-구조와-기술-스택.md) 의 "문서의 다이어그램" 절에 있다.
- 2026-09-05 — 여덟 장의 다이어그램 형식을 바꿨다. Mermaid 인라인을 걷어내고 `documents/diagrams/` 아래 JSON 사양 + 탐색 가능한 HTML + 인라인용 PNG 세 벌로 다시 그렸다. 문서 본문에는 PNG 가 보이고 그 아래 HTML 링크가 붙는다. 그림이 지고 있던 주장은 그대로이고, 미결이던 것은 그림 안에서도 미결로 남아 있다. 새 규칙은 [저장소 구조와 기술 스택](저장소-구조와-기술-스택.md) 의 "문서의 다이어그램" 절에 있다.

- 2026-09-06 — 에픽 KAN-13(프로토타입 쿠폰 발급)의 설계문서를 [KAN-13/design.md](KAN-13/design.md) 로 작성했다. 발급 신호는 랜덤 하나만 구현하되 가중치 결합 구조는 유지하고, 트리거는 시연 버튼, 후보 데이터는 정적 시드로 축소한 범위다. 구현 브랜치 일곱 개의 스택·API 계약·RED 테스트가 정의되어 있고, 구현은 아직 시작하지 않았다.
- 2026-09-06 — 설계문서 13장 티켓맵대로 지라에 구현 티켓 7건(KAN-14~KAN-20)을 에픽 KAN-13 아래 생성하고, 에픽 본문에 설계 요약을 반영했다. 대응 관계와 부모 규칙 변경은 [지라 티켓 연결](지라-티켓-연결.md) 에 있다.
- 2026-09-07 — 에픽 KAN-13(프로토타입 쿠폰 발급)의 구현이 섰다. 구현 브랜치 일곱(`KAN-13/01-seed-and-contracts` ~ `07-issuance-e2e`)에 커밋 28건이 쌓였고, 화면 둘(발급 실행 · 내 쿠폰)이 `POST /api/coupons/issue`·`GET /api/coupons?ownerId=`·`GET /api/citizens` 를 거쳐 JSON 파일 DB 까지 관통한다. 랜덤 신호 하나로 가중치를 결합해 가맹점×시민 후보에서 한 건을 골라 발급하고, 발급된 쿠폰은 거래조건 고지와 함께 그 소유자의 내 쿠폰 화면에 보인다. 단위 테스트 241건(api 88 · web 142 · db 11)과 e2e 4건이 통과하고 `typecheck` 는 7/7 이다. 소비 트랙과 나머지 네 신호는 범위 밖이다. **일곱 브랜치는 아직 어느 것도 머지·푸시되지 않은 스택 상태로, 이 저장소의 `main` 에는 아직 반영되지 않았다.**

## 미결

구현에 직접 걸리는 것만 둔다.

- [ ] **기획 노선 확정 대기.** 공용 풀·점유(찜하기)·블록체인 축 세 가지를 함께 정해야 하고, 이것이 나머지 미결 전체의 선행이다. 선택지와 찬반 근거는 [쿠폰 도메인 규칙](쿠폰-도메인-규칙.md) 미결에 있다. 이 결정이 `packages/db` 의 동시 쓰기 보호 필요 여부까지 가른다. 2026-09-07 — 그중 `coupons` 쓰기의 보호는 에픽 KAN-13 이 프로세스 내 직렬화로 닫았고, 노선 결정에 매달린 채 남은 것은 프로세스 밖까지 막는 파일 락뿐이다. 상세는 [저장소 구조와 기술 스택](저장소-구조와-기술-스택.md) 의 결정과 미결에 있다
- [ ] 소유자 보상의 비환금화 방식을 택일한다 — 발급 우선권 / 기여도 점수 / 봉사·기부 마일리지. 상세는 [쿠폰 도메인 규칙](쿠폰-도메인-규칙.md)
- [ ] 타겟팅 판별식을 수치화한다. 발급 알고리즘의 입력이 여기서 정해진다
- [ ] 프로토타입 제출 형태(구현 코드 / 시연 영상)와 그에 따른 실행·배포 절차를 정한다. 상세는 [프로토타입 범위](프로토타입-범위.md)

## 이 파일을 갱신하는 규칙

- 문서를 만들거나 지웠으면 "문서 목록"을 같은 커밋에서 갱신한다. 목록에 없는 문서는 없는 문서다.
- 결정은 각 주제 문서에 `- YYYY-MM-DD — <결정 한 줄>` 로 쌓는다. 뒤집힌 결정도 지우지 않고 뒤집은 사실을 새 날짜로 적는다.
- 날짜는 절대 날짜로 적는다. "어제", "지난주"는 쓰지 않는다.
- 맨 위 "갱신" 날짜를 함께 고친다.
- 근거는 다른 곳에 미루지 않고 여기에 직접 옮겨 적는다 (`documents-first` §6).

- [KAN-13 PR별 변경 흐름과 시각화](KAN-13/pr-review/README.md) — PR #8~#15의 PNG·SVG·HTML 및 검증 기록
